// 2 groups of 3 digits with a dash and a thrid group of 4 digits
export const PhoneRegEx = /^(?:[0-9]{3}-){2}[0-9]{4}$/;

// 5 required digits followed by an optional dash and 4 digits
export const ZipCodeRegEx = /^[0-9]{5}(?:-[0-9]{4})?$/;
